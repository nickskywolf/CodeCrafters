import calendar
import csv
import json
import os
import string
from collections import UserDict
from datetime import datetime
import re

from addressbook import AddressBook, Name, Phone, Email, Address, Birthday, Record
from addressbook import run as run_adressbook

# from NotebookD import note_book_function
# from file_sorter import parser_function

def main_menu():
    while True:
        os.system('cls' if os.name == 'nt' else 'clear')  # Очистити екран, в залежності від операційної системи

        print("Main Menu:")
        print("1. Launch Address Book")
        print("2. Launch Notepad")
        print("3. Launch File Sorter")
        print("4. Exit")

        choice = input("Enter your choice: ")

        if choice == '1':
            launch_address_book()
        elif choice == '2':
            launch_notepad()
        elif choice == '3':
            launch_file_sorter()
        elif choice == '4':
            break
        else:
            print("Invalid choice. Please try again.")

def launch_address_book():
    print("Launching Address Book...")
    run_adressbook()

def launch_notepad():
    print("Launching Notepad...")
    # Виклик функції для запуску Блокноту

def launch_file_sorter():
    print("Launching File Sorter...")
    # Виклик функції для запуску Сортувальника Файлів

if __name__ == "__main__":
    main_menu()
